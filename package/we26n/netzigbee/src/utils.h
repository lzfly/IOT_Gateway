#ifndef BLST_UTILS_H
#define BLST_UTILS_H

#define BLST_TRUE (1)
#define BLST_FALSE (0)

extern char* dupstr(char* s);
extern char* strip(char* org);
extern int endsWith(char* s, char* s1);
extern int startsWith(char* s, char* s1);

#endif
